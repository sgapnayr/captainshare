
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Hello {
  id    String @id @default(cuid()) @map("_id")
  name  String
}